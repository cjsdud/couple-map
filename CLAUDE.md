# CLAUDE.md — 우리의 도화지 (커플 앱)

이 파일은 Claude Code가 이 프로젝트를 작업할 때의 최상위 지침이다.
상세 근거는 반드시 다음 문서를 먼저 읽는다:
- `docs/couple-app-spec-v0.4.md` — 기능 명세 (무엇을 만드는가)
- `docs/dohwaji-tech-design-v0.1.md` — 기술 설계 (어떻게 만드는가)

## 프로젝트 한 줄

한국 20대 커플용 앱. 지도(시군구 정복·데이트 기록)가 홈, 오늘(상호 잠금 일상 공유)이 리텐션 루프, 우리(디데이·가계부)가 관계 자산. v1은 앱인토스 WebView 미니앱(100% 무료), 이후 스토어 네이티브(Capacitor).

## 절대 규칙 (위반 금지)

1. 화면은 지도/오늘/우리 3개뿐. 새 화면 추가 금지 — 레이어·카드·모달로만 확장.
2. 상호 잠금(오늘 사진·질문 답)은 **반드시 Postgres RLS로 강제**한다. 클라이언트 분기만으로 구현 금지. 정책 SQL은 기술 설계 §5 참조.
3. 루프 기능(핀·오늘 업로드·질문·잔디)에 과금벽 금지.
4. 사진 업로드 전 클라이언트에서 EXIF GPS 제거. 위치는 별도 필드로만 저장.
5. 모든 테이블 RLS: `couple_id` 밖의 데이터는 어떤 경로로도 노출 금지. Storage도 경로 기반 동일.
6. 정복 판정은 status='visited' 데이트 핀만. 오늘 사진은 지도에 올리지 않는다 (핀 승격 경로만).
7. 스택 임의 변경 금지: React 18 + Vite + TS + Tailwind + TanStack Query + Supabase + Vercel. 새 라이브러리 추가는 근거를 커밋 메시지에 남긴다.
8. 밸런스·비교 UI 톤: 정산 압박·경고색·리더보드 금지 (명세 §3.3, §5.4).

## 빌드 순서와 게이트 (순서 변경 금지)

### Phase 0 — 스파이크 (다른 작업보다 먼저, 반나절 목표)
목적: 아키텍처를 흔들 수 있는 앱인토스 변수 2개를 최소 코드로 검증.
- [ ] "Hello 도화지" 페이지를 Vercel에 배포하고, 그 안에서 ①Supabase 익명 쿼리 fetch ②Kakao Local REST 호출 ③geolocation 권한 요청이 앱인토스 샌드박스 WebView에서 동작하는지 확인.
- [ ] 앱인토스 로그인 정책 확인 (토스 로그인 강제 여부). 강제라면 tech-design §12-1의 매핑 레이어를 M0 범위에 추가.
- 결과를 `docs/spike-result.md`에 기록하고, 실패 항목이 있으면 **작업을 멈추고 사용자와 대안을 결정한다.**

### M0 — 기반
- 레포 구조(tech-design §9), CI(lint·typecheck·build), Vercel 연동.
- Supabase 마이그레이션(스키마 전체, tech-design §4) + RLS 정책 + seed(sigungu 229, questions 730 — questions는 생성 후 사용자 검수 요청).
- 카카오 OAuth 로그인, 커플 연결(초대 코드 발급/입력, pending→active).
- DoD: 두 계정으로 커플 연결이 실기기 브라우저에서 성공.

### M1 — 지도
- 시군구 GeoJSON 단순화본 준비(`public/geo/`), SVG 정복 지도(색칠 + 방문 횟수 덧칠 진하기).
- 기록 CRUD: 다녀왔어요/가고 싶어요 분기, 스팟 1~5(검색 + 지금 여기), 사진, 메모, 가계부 입력(카테고리 5 고정).
- coord2regioncode로 sigungu_code 저장, 정복률 계산, 동일 장소 클러스터(×N), 지도↔타임라인 토글(오늘 기록 통합 필터 포함).
- DoD: 실제 데이트 1건(스팟 2개)을 입력하면 지도 색칠·정복률·타임라인·지출이 전부 반영.

### M2 — 오늘
- 업로드 파이프라인(압축 1,600px WebP, 경로 규칙, signed URL), 1일 30장 상한.
- 상호 잠금 RLS 적용 + 잠금 카드 상시 설명 문구, 질문(양방 잠금), 기분, 잔디, 스트릭, 커플별 마감(day_cutoff).
- DoD: 계정 A만 올린 상태에서 B의 API 직접 호출로도 A 사진이 안 보이고, B가 올리는 순간 열린다 (RLS 테스트 코드 포함).

### M3 — 우리 + 제출
- 디데이·기념일(100일 자동 생성), 가계부 월간 카드(합계·횟수·평균·비율 밸런스), 설정(마감·비율·알림).
- 알림 연동(앱인토스 푸시 API, 일 3건 상한 로그), 다크패턴 정책 점검(진입 즉시 바텀시트 금지 등).
- 앱인토스 패키징(.ait)·샌드박스 통합 테스트·콘솔 검토 요청.
- DoD: 샌드박스에서 온보딩→핀→오늘 루프→우리 전체 흐름 통과.

## 디자인 토큰 (프로토타입 계승)

`docs/dohwaji-prototype-v1.html`이 비주얼 기준. Tailwind config로 이식:
- paper `#FDFCF7` / ink `#3B3733` / pink `#E8637C` / green `#8CAB68` / sky `#9EC3D8` / yellow `#F2C14E`
- 손그림 언어: 비대칭 border-radius, 크레용 빗금 패턴(덧칠 = 진하기), SVG wobble 필터, 마스킹테이프 모티프.
- 카피 톤: 짝꿍·콕·도화지. 정산·경고 어휘 금지.

## 사용자(박천영)에게 요청해야 하는 것들

Claude Code가 스스로 못 하는 계정 작업. 필요한 시점에 명확히 요청할 것:
- Supabase 프로젝트 생성 + URL/anon key
- Kakao Developers 앱 등록(REST 키·JS 키·OAuth 리다이렉트·사이트 도메인) — **주의: 무료 쿼터는 계정당 첫 번째 지도 활성화 앱. 이력 있는 계정이면 신규 계정 권장**
- Vercel 프로젝트 연결, GitHub 레포 생성
- 앱인토스 콘솔 등록·샌드박스 설치·검토 요청 제출
- questions 730개 생성본 검수

## 작업 방식

- 사용자는 모바일 단독 환경. 로컬 데스크톱 전제의 지시(예: "로컬에서 시뮬레이터 실행") 금지. 실행 확인은 Vercel preview URL 기준.
- 커밋은 마일스톤 내 작업 단위로 잘게, 메시지는 한국어 요약 + 범위 태그(`[M1] 정복 지도 덧칠 렌더`).
- 각 마일스톤 DoD 충족 시 사용자에게 확인을 받고 다음으로 넘어간다. Phase 0 실패 항목은 임의 우회하지 않는다.
